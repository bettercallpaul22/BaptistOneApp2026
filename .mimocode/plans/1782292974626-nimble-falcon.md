# Plan: Update Email Verify Path

## Goal
Change the email verification URL from `/auth/register/verify-email` to `/auth/email/verify`.

## Files to Modify

### 1. `src/routes/paths.ts` (line 60)
- Change: `registerVerification: '/auth/register/verify-email'` → `registerVerification: '/auth/email/verify'`

### 2. `.env` (lines 8-9)
- Change: `VITE_REGISTER_REDIRECT_PATH=/auth/register/verify-email` → `VITE_REGISTER_REDIRECT_PATH=/auth/email/verify`
- Update the comment on line 8 accordingly

### No other changes needed
- `src/routes/index.tsx:265` — uses `paths.registerVerification`, will pick up the new value automatically
- `src/constants/callbackUrls.ts:23` — uses `paths.registerVerification`, will pick up automatically
- `src/pages/auth/register/RegisterPage.tsx:103` — uses `paths.registerVerification`, will pick up automatically
- `src/config/auth.ts:15` — uses `callbackUrls.registerVerification()`, will pick up automatically

## Verification
- Run `npm run build` (or equivalent) to confirm no broken imports/types
- Visually verify the paths object exports the new URL
